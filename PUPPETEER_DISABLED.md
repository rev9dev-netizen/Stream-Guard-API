# Puppeteer is temporarily disabled to run on free tier (512MB RAM)

# To re-enable Puppeteer:
# 1. Uncomment the Puppeteer installation in Dockerfile
# 2. Upgrade to a paid tier with at least 1GB RAM
# 3. Or use Railway.app (8GB free tier)

# Providers that need Puppeteer (currently disabled):
# - FshareTV (Cloudflare bypass)
# - Any provider with heavy JavaScript rendering

# All other providers will work normally without Puppeteer
