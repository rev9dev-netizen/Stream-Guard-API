name = "tmdb-proxy"
main = "src/index.ts"
compatibility_date = "2024-01-01"

# Development settings
[dev]
ip = "0.0.0.0"
port = 8788
local_protocol = "http"

# IMPORTANT: Use --local flag for local development
# Example: npx wrangler dev --local --port 8788
